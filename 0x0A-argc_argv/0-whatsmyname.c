#include "main.h"
#include <stdio.h>

/**
 * main - Print name followed by \n
 * @argc: Hold arguments passeed
 * @argv: ptr arr for arg(s)
 *
 * Return: Always (0) success
*/

int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);

	return (0);
}
